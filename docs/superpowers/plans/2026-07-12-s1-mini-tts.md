# OpenAudio S1-mini TTS Implementation Plan

> **For agentic workers:** план исполняется workflow-оркестрацией (задача = отдельный агент, ревью между волнами). Шаги — чекбоксы `- [ ]`. Спека: `docs/superpowers/specs/2026-07-12-s1-mini-tts-design.md`.

**Goal:** заменить Silero на OpenAudio S1-mini — эмоциональный русский голос на GPU через worker-подпроцесс, с докачкой рантайма/весов, лоадерами в панели и гарантированной выгрузкой VRAM при закрытии.

**Architecture:** основное приложение не импортирует torch. `bootstrap.py` докачивает `gpu-runtime/` (pip --target) и `models/s1-mini/` (зеркало GitHub Releases, SHA256). `client.py` спавнит `worker.py` (stdlib + gpu-runtime, мини-HTTP на 127.0.0.1) и терминирует его при закрытии; worker самоуничтожается по EOF stdin.

**Tech Stack:** Python 3.12 (embedded в portable), fish-speech + CUDA-torch (только в gpu-runtime), FastAPI, httpx, React/Vite.

## Global Constraints

- **Тестов нет и не создаются** (правило пользователя). Верификация — `py_compile`, `python -c`, `npm run build`, ручные прогоны.
- **Не коммитить** (правило пользователя). Шагов `git commit` в плане нет.
- Комментарии в коде и тексты UI — по-русски, в стиле существующих файлов (комментарии объясняют «почему», не «что»).
- `worker.py` импортирует только stdlib и пакеты из `gpu-runtime/`; ничего из `stream_director` и основного site-packages.
- Основное приложение (вне `gpu-runtime/`) не получает зависимостей на torch/fish-speech.
- Долгие операции — только в потоках executor (event loop не блокировать).
- Статус-объект всегда вида `{"state": str, "progress": dict | None, "error": str | None}`; state ∈ `checking | no_gpu | downloading_runtime | downloading_model | starting | loading | ready | error`.
- Лицензия: OpenAudio S1-mini © Fish Audio, CC-BY-NC-SA 4.0, использование некоммерческое — атрибуция в README и в нотах релиза с весами.
- Репозиторий зеркала весов: `https://github.com/Lakai4eg/game-ai-commentator/releases/download/models-s1-mini-v1/<файл>`.

---

### Task 1: Spike — валидация GPU-стека на живой машине

Цель: подтвердить, что fish-speech ставится через `pip --target` под Python 3.12 на Windows и его Python-API соответствует коду worker-а (Task 5). Веса не нужны (их даёт Task 11) — проверяем установку и импорты.

**Files:**
- Create: `build/spike-runtime/` (временный каталог, в git не попадает — `build/` в .gitignore)
- Create: `docs/superpowers/plans/spike-results.md` (протокол: что встало, точные пины, фактические имена API)

**Interfaces:**
- Produces: проверенные значения для `pins.py` (Task 2): список `RUNTIME_PACKAGES` с точными версиями, `TORCH_INDEX_URL`; подтверждённые импорты для `worker.py` (Task 5).

- [x] **Step 1: установить стек в изолированный target-каталог** (фоново, скачивается ~3 ГБ)

```
python -m pip install --target build/spike-runtime --index-url https://download.pytorch.org/whl/cu126 --extra-index-url https://pypi.org/simple torch fish-speech
```

Если пакет `fish-speech` с PyPI (там версия 0.1.0) не содержит `fish_speech.inference_engine` — ставить с git-тега: `pip install --target build/spike-runtime "fish-speech @ git+https://github.com/fishaudio/fish-speech@<последний тег>"` и зафиксировать тег в протоколе.

- [x] **Step 2: проверить импорты API, который использует worker**

```
python -c "import sys; sys.path.insert(0, 'build/spike-runtime'); import torch; from fish_speech.inference_engine import TTSInferenceEngine; from fish_speech.models.dac.inference import load_model; from fish_speech.models.text2semantic.inference import launch_thread_safe_queue; from fish_speech.utils.schema import ServeTTSRequest, ServeReferenceAudio; print('ok', torch.__version__, torch.cuda.is_available())"
```

Expected: `ok <версия> True`. Если импорты называются иначе — записать фактические пути импортов в `spike-results.md` (Task 5 применит их).

- [x] **Step 3: записать результаты**

`spike-results.md`: точные версии (`pip list --path build/spike-runtime`), источник fish-speech (PyPI/git-тег), фактические импорты, суммарный размер каталога. Указать выбранный CUDA-индекс (cu126; если torch для него не нашёлся — cu124, тоже записать).

---

### Task 2: Пакет tts — каркас: маркеры, голоса, аудио-кольцо, пины

**Files:**
- Create: `src/stream_director/tts/__init__.py`
- Create: `src/stream_director/tts/markers.py`
- Create: `src/stream_director/tts/voices.py`
- Create: `src/stream_director/tts/audio.py`
- Create: `src/stream_director/tts/pins.py`
- Modify: `.gitignore` (добавить `gpu-runtime/` и `models/`)

**Interfaces:**
- Produces:
  - `EMOTION_MARKERS: tuple[str, ...]`, `strip_markers(text: str) -> str`
  - `DEFAULT_VOICE = "default"`, `VOICES_DIR: Path`, `list_voices() -> list[str]`, `save_voice(name: str, wav: bytes, transcript: str) -> None` (ValueError при плохом имени/транскрипте), `delete_voice(name: str) -> bool`, `voice_paths(name: str) -> tuple[Path, Path] | None`, `pick_voice(settings, stim_type: str, priority) -> str`
  - `AudioStore` (перенос из старого `tts.py` без изменений поведения)
  - `pins.py`: `TORCH_INDEX_URL: str`, `RUNTIME_PACKAGES: list[str]`, `WEIGHTS_BASE_URL: str`, `WEIGHTS: dict[str, tuple[str, int]]` (имя → (sha256, размер в байтах))

- [x] **Step 1: `markers.py`**

```python
"""Эмо-маркеры OpenAudio S1: список для промпта и вырезание из текста оверлея."""

from __future__ import annotations

import re

# Подмножество поддерживаемых моделью маркеров: короткое, чтобы не размывать
# промпт. Ровно этот список попадает в инструкцию LLM (commentary/defaults.py).
EMOTION_MARKERS: tuple[str, ...] = (
    "angry", "excited", "disdainful", "surprised", "confident", "sad",
    "whispering", "shouting", "laughing", "chuckling", "sighing",
)

_MARKER_RE = re.compile(r"\((?:%s)\)\s*" % "|".join(EMOTION_MARKERS))


def strip_markers(text: str) -> str:
    """Убрать известные маркеры: зрителю — чистый текст, движку — с маркерами.

    Незнакомые скобки не трогаем: LLM может законно скобки использовать.
    """
    return _MARKER_RE.sub("", text).strip()
```

- [x] **Step 2: `voices.py`**

```python
"""Референсы голосов: data/voices/<имя>.wav + <имя>.txt (транскрипт)."""

from __future__ import annotations

import re
from pathlib import Path

VOICES_DIR = Path("data") / "voices"
# Голос без референса — собственный тембр модели.
DEFAULT_VOICE = "default"
_NAME_RE = re.compile(r"^[a-zA-Zа-яА-ЯёЁ0-9_-]{1,32}$")


def list_voices() -> list[str]:
    names = []
    if VOICES_DIR.is_dir():
        for wav in VOICES_DIR.glob("*.wav"):
            if wav.with_suffix(".txt").is_file():
                names.append(wav.stem)
    return [DEFAULT_VOICE, *sorted(names)]


def save_voice(name: str, wav: bytes, transcript: str) -> None:
    if not _NAME_RE.match(name) or name == DEFAULT_VOICE:
        raise ValueError("имя: буквы/цифры/дефис/подчёркивание, до 32 символов")
    if not transcript.strip():
        raise ValueError("нужен текст-транскрипт референса")
    VOICES_DIR.mkdir(parents=True, exist_ok=True)
    (VOICES_DIR / f"{name}.wav").write_bytes(wav)
    (VOICES_DIR / f"{name}.txt").write_text(transcript.strip(), encoding="utf-8")


def delete_voice(name: str) -> bool:
    paths = voice_paths(name)
    if paths is None:
        return False
    for p in paths:
        p.unlink(missing_ok=True)
    return True


def voice_paths(name: str) -> tuple[Path, Path] | None:
    """(wav, txt) существующего референса; default и незнакомые имена — None."""
    if name == DEFAULT_VOICE or not _NAME_RE.match(name):
        return None
    wav, txt = VOICES_DIR / f"{name}.wav", VOICES_DIR / f"{name}.txt"
    return (wav, txt) if wav.is_file() and txt.is_file() else None


def pick_voice(settings, stim_type: str, priority) -> str:
    """Голос под контекст: override по типу > правило по приоритету > дефолт.

    Имя без файлов референса игнорируется на своём уровне — синтез не падает.
    """
    known = set(list_voices())
    for candidate in (
        settings.voice_overrides.get(stim_type),
        settings.voice_by_priority.get(priority.name.lower()),
        settings.default_voice,
    ):
        if candidate in known:
            return candidate
    return DEFAULT_VOICE
```

- [x] **Step 3: `audio.py`** — перенести класс `AudioStore` из `src/stream_director/tts.py` (строки 102–121) дословно, с докстрингом модуля `"""Кольцо последних WAV для отдачи оверлею по id."""`. Старый файл пока не удалять (удаляет Task 7).

- [x] **Step 4: `pins.py`**

```python
"""Пины GPU-стека: версии пакетов, индекс torch, манифест весов.

Значения меняются только после проверки на живой машине (spike / Task 11).
Пустой sha256 — маркер «зеркало ещё не опубликовано»: bootstrap честно
останавливается с ошибкой, а не качает непроверенное.
"""

TORCH_INDEX_URL = "https://download.pytorch.org/whl/cu126"  # из spike-results.md
RUNTIME_PACKAGES = [
    # Точные версии — из spike-results.md (Task 1).
    "torch==<из spike>",
    "fish-speech==<из spike>",
]

WEIGHTS_BASE_URL = (
    "https://github.com/Lakai4eg/game-ai-commentator/releases/download/models-s1-mini-v1"
)
# имя файла → (sha256, размер в байтах); значения вписывает Task 11.
WEIGHTS: dict[str, tuple[str, int]] = {
    "model.pth": ("", 0),
    "codec.pth": ("", 0),
    "tokenizer.tiktoken": ("", 0),
    "special_tokens.json": ("", 0),
    "config.json": ("", 0),
}
```

Если `spike-results.md` уже существует (Task 1 завершён) — вписать реальные версии вместо `<из spike>`; иначе оставить как есть, Task 11 допишет.

- [x] **Step 5: `__init__.py`**

```python
"""Озвучка OpenAudio S1-mini: worker-подпроцесс на GPU + вспомогательные части."""

from .audio import AudioStore
from .client import S1MiniTTS
from .markers import EMOTION_MARKERS, strip_markers
from .voices import DEFAULT_VOICE, delete_voice, list_voices, pick_voice, save_voice

__all__ = [
    "AudioStore", "S1MiniTTS", "EMOTION_MARKERS", "strip_markers",
    "DEFAULT_VOICE", "delete_voice", "list_voices", "pick_voice", "save_voice",
]
```

Импорт `client` появится в Task 4; до него `__init__.py` создать без строки `from .client import S1MiniTTS` и без `"S1MiniTTS"` в `__all__` — Task 4 добавит.

- [x] **Step 6: `.gitignore`** — добавить строки `gpu-runtime/` и `models/` после строки `data/`.

- [x] **Step 7: верификация**

```
python -m py_compile src/stream_director/tts/markers.py src/stream_director/tts/voices.py src/stream_director/tts/audio.py src/stream_director/tts/pins.py
python -c "import sys; sys.path.insert(0,'src'); from stream_director.tts.markers import strip_markers; assert strip_markers('(angry) Ну ты дал!') == 'Ну ты дал!'; assert strip_markers('без маркера (скобки)') == 'без маркера (скобки)'; print('ok')"
```

Expected: `ok`.

---

### Task 3: bootstrap.py — GPU-проверка и докачки

**Files:**
- Create: `src/stream_director/tts/bootstrap.py`

**Interfaces:**
- Consumes: `pins.py` (Task 2).
- Produces:
  - `RUNTIME_DIR = Path("gpu-runtime")`, `MODEL_DIR = Path("models") / "s1-mini"`
  - `class BootstrapError(Exception)`
  - `check_gpu() -> str | None` — None если NVIDIA GPU виден, иначе текст причины
  - `ensure_runtime(status: Callable[[dict], None]) -> None` — ставит пакеты, шлёт `downloading_runtime` + `progress.step`; бросает BootstrapError
  - `ensure_weights(status: Callable[[dict], None]) -> None` — качает веса, шлёт `downloading_model` + `progress.done_mb/total_mb`; бросает BootstrapError

- [x] **Step 1: написать модуль**

```python
"""Первый старт: проверка GPU, установка gpu-runtime, докачка весов S1-mini.

Всё синхронное — вызывается из потока executor, event loop не трогает.
"""

from __future__ import annotations

import hashlib
import json
import logging
import re
import shutil
import subprocess
import sys
from pathlib import Path
from typing import Callable

import httpx

from .pins import RUNTIME_PACKAGES, TORCH_INDEX_URL, WEIGHTS, WEIGHTS_BASE_URL

log = logging.getLogger(__name__)

RUNTIME_DIR = Path("gpu-runtime")
MODEL_DIR = Path("models") / "s1-mini"
# Windows: не показывать консольное окно дочернего pip.
CREATE_NO_WINDOW = 0x08000000 if sys.platform == "win32" else 0

StatusCb = Callable[[dict], None]


class BootstrapError(Exception):
    """Человекочитаемая причина, почему голос не поднялся."""


def _st(state: str, progress: dict | None = None, error: str | None = None) -> dict:
    return {"state": state, "progress": progress, "error": error}


def check_gpu() -> str | None:
    try:
        out = subprocess.run(
            ["nvidia-smi", "-L"], capture_output=True, text=True, timeout=15,
            creationflags=CREATE_NO_WINDOW,
        )
    except (OSError, subprocess.TimeoutExpired):
        return "nvidia-smi не найден — для голоса нужна видеокарта NVIDIA"
    if out.returncode != 0 or "GPU" not in out.stdout:
        return "NVIDIA GPU не обнаружен — голос работать не будет"
    return None


def _pins_fingerprint() -> str:
    raw = json.dumps([TORCH_INDEX_URL, RUNTIME_PACKAGES], ensure_ascii=False)
    return hashlib.sha256(raw.encode()).hexdigest()


def ensure_runtime(status: StatusCb) -> None:
    marker = RUNTIME_DIR / ".complete"
    if marker.is_file() and marker.read_text() == _pins_fingerprint():
        return
    # Пины сменились — старый рантайм несовместим, ставим с нуля.
    if RUNTIME_DIR.exists():
        shutil.rmtree(RUNTIME_DIR)
    status(_st("downloading_runtime", {"step": "подготовка…"}))
    cmd = [
        sys.executable, "-m", "pip", "install",
        "--target", str(RUNTIME_DIR),
        "--index-url", TORCH_INDEX_URL,
        "--extra-index-url", "https://pypi.org/simple",
        "--progress-bar", "off",
        *RUNTIME_PACKAGES,
    ]
    proc = subprocess.Popen(
        cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True,
        encoding="utf-8", errors="replace", creationflags=CREATE_NO_WINDOW,
    )
    tail: list[str] = []
    for line in proc.stdout:
        tail.append(line.rstrip())
        tail = tail[-30:]
        m = re.match(r"\s*(Collecting|Downloading)\s+(\S+)", line)
        if m:
            status(_st("downloading_runtime", {"step": m.group(2)}))
    if proc.wait() != 0:
        raise BootstrapError("pip не смог установить GPU-рантайм:\n" + "\n".join(tail[-8:]))
    marker.write_text(_pins_fingerprint())


def _sha256(path: Path) -> str:
    h = hashlib.sha256()
    with path.open("rb") as f:
        for chunk in iter(lambda: f.read(1 << 20), b""):
            h.update(chunk)
    return h.hexdigest()


def ensure_weights(status: StatusCb) -> None:
    if any(not sha for sha, _ in WEIGHTS.values()):
        raise BootstrapError("зеркало весов не опубликовано (pins.py пуст) — соберите релиз моделей")
    MODEL_DIR.mkdir(parents=True, exist_ok=True)
    total_mb = sum(size for _, size in WEIGHTS.values()) // 2**20
    done = 0
    for name, (sha, size) in WEIGHTS.items():
        dest = MODEL_DIR / name
        ok_marker = MODEL_DIR / f"{name}.ok"
        if dest.is_file() and ok_marker.is_file():
            done += size
            continue
        _download(f"{WEIGHTS_BASE_URL}/{name}", dest, done, total_mb, status)
        if _sha256(dest) != sha:
            dest.unlink()
            raise BootstrapError(f"SHA256 не совпал для {name} — файл повреждён, перезапустите")
        ok_marker.write_text("ok")
        done += size


def _download(url: str, dest: Path, done_before: int, total_mb: int, status: StatusCb) -> None:
    """Докачка с Range: обрыв сети не заставляет качать гигабайты заново."""
    tmp = dest.with_suffix(dest.suffix + ".tmp")
    offset = tmp.stat().st_size if tmp.exists() else 0
    headers = {"Range": f"bytes={offset}-"} if offset else {}
    try:
        with httpx.stream("GET", url, headers=headers, follow_redirects=True,
                          timeout=httpx.Timeout(30.0, read=120.0)) as r:
            if r.status_code == 200:  # сервер не умеет Range — начинаем заново
                offset = 0
            elif r.status_code != 206 and offset:
                raise BootstrapError(f"зеркало ответило {r.status_code} на {url}")
            r.raise_for_status()
            mode = "ab" if offset else "wb"
            with tmp.open(mode) as f:
                for chunk in r.iter_bytes(1 << 20):
                    f.write(chunk)
                    offset += len(chunk)
                    status(_st("downloading_model", {
                        "done_mb": (done_before + offset) // 2**20,
                        "total_mb": total_mb,
                    }))
    except httpx.HTTPError as e:
        raise BootstrapError(f"обрыв скачивания {dest.name}: {e}") from e
    tmp.rename(dest)
```

- [x] **Step 2: верификация**

```
python -m py_compile src/stream_director/tts/bootstrap.py
python -c "import sys; sys.path.insert(0,'src'); from stream_director.tts.bootstrap import check_gpu, BootstrapError, ensure_weights; err = check_gpu(); print('gpu:', err or 'ok');
try:
    ensure_weights(lambda s: None)
except BootstrapError as e:
    print('ожидаемо:', e)"
```

Expected: `gpu: ok` (на машине с NVIDIA) и `ожидаемо: зеркало весов не опубликовано…` (пины ещё пустые).

---

### Task 4: client.py — S1MiniTTS: спавн worker-а, health, synth, stop

**Files:**
- Create: `src/stream_director/tts/client.py`
- Modify: `src/stream_director/tts/__init__.py` (добавить `from .client import S1MiniTTS` и `"S1MiniTTS"` в `__all__`)

**Interfaces:**
- Consumes: `bootstrap.check_gpu/ensure_runtime/ensure_weights/BootstrapError/RUNTIME_DIR/MODEL_DIR` (Task 3), `voices.VOICES_DIR` (Task 2), `worker.py` как файл рядом (Task 5).
- Produces: `class S1MiniTTS`:
  - `__init__(self, on_status: Callable[[dict], None])`
  - `available: bool` (property)
  - `start() -> None` — синхронный, для executor; повторный вызов во время работы — no-op
  - `synth(text: str, voice: str | None = None) -> bytes | None` — синхронный, WAV или None
  - `stop() -> None` — терминирует worker

- [x] **Step 1: написать модуль**

```python
"""Клиент голосового worker-а: жизненный цикл подпроцесса и HTTP-вызовы."""

from __future__ import annotations

import logging
import socket
import subprocess
import sys
import threading
import time
from pathlib import Path
from typing import Callable

import httpx

from . import bootstrap
from .bootstrap import CREATE_NO_WINDOW, MODEL_DIR, RUNTIME_DIR, BootstrapError
from .voices import VOICES_DIR

log = logging.getLogger(__name__)

WORKER_PATH = Path(__file__).parent / "worker.py"
# Модель разворачивается в VRAM до пары минут на медленных дисках.
READY_TIMEOUT_S = 240.0
SYNTH_TIMEOUT_S = 90.0
MAX_RESTARTS = 3


class S1MiniTTS:
    """Голос приложения. Недоступен — реплики идут текстом, это штатно."""

    def __init__(self, on_status: Callable[[dict], None]):
        self._status_cb = on_status
        self._proc: subprocess.Popen | None = None
        self._port: int | None = None
        self._ready = False
        self._starting = threading.Lock()
        self._restarts = 0
        self._http = httpx.Client(timeout=SYNTH_TIMEOUT_S)
        self._set("checking")

    @property
    def available(self) -> bool:
        return self._ready

    def _set(self, state: str, progress: dict | None = None, error: str | None = None) -> None:
        self._status_cb({"state": state, "progress": progress, "error": error})

    def start(self) -> None:
        """Bootstrap + спавн. Идемпотентен: параллельный вызов просто выходит."""
        if not self._starting.acquire(blocking=False):
            return
        try:
            self._ready = False
            self._restarts = 0
            reason = bootstrap.check_gpu()
            if reason is not None:
                self._set("no_gpu", error=reason)
                return
            bootstrap.ensure_runtime(self._status_cb)
            bootstrap.ensure_weights(self._status_cb)
            self._spawn_and_wait()
        except BootstrapError as e:
            self._set("error", error=str(e))
        except Exception as e:
            log.exception("Старт голоса упал")
            self._set("error", error=f"не удалось запустить голос: {e}")
        finally:
            self._starting.release()

    def _spawn_and_wait(self) -> None:
        self._set("starting")
        self._kill_proc()
        with socket.socket() as s:  # свободный порт: без гонок в пределах локалхоста
            s.bind(("127.0.0.1", 0))
            self._port = s.getsockname()[1]
        self._proc = subprocess.Popen(
            [sys.executable, str(WORKER_PATH),
             "--runtime", str(RUNTIME_DIR), "--model-dir", str(MODEL_DIR),
             "--voices-dir", str(VOICES_DIR), "--port", str(self._port)],
            stdin=subprocess.PIPE,  # worker умирает по EOF — страховка от сирот
            creationflags=CREATE_NO_WINDOW,
        )
        deadline = time.monotonic() + READY_TIMEOUT_S
        while time.monotonic() < deadline:
            if self._proc.poll() is not None:
                self._set("error", error=f"голосовой движок завершился (код {self._proc.returncode})")
                return
            try:
                phase = self._http.get(self._url("/health"), timeout=2.0).json()["phase"]
            except (httpx.HTTPError, KeyError, ValueError):
                time.sleep(0.5)
                continue
            if phase == "ready":
                self._ready = True
                self._set("ready")
                return
            self._set("loading")
            time.sleep(0.5)
        self._set("error", error="голосовой движок не поднялся за отведённое время")

    def _url(self, path: str) -> str:
        return f"http://127.0.0.1:{self._port}{path}"

    def synth(self, text: str, voice: str | None = None) -> bytes | None:
        if not self._ready:
            return None
        try:
            r = self._http.post(self._url("/synth"), json={"text": text, "voice": voice})
            if r.status_code != 200:
                log.warning("synth %s: %s", r.status_code, r.text[:200])
                return None
            return r.content
        except httpx.HTTPError:
            log.warning("Голосовой worker не отвечает — перезапуск в фоне")
            self._ready = False
            threading.Thread(target=self._restart, daemon=True).start()
            return None

    def _restart(self) -> None:
        if self._restarts >= MAX_RESTARTS:
            self._set("error", error="голос падает раз за разом — нажмите «повторить» в панели")
            return
        self._restarts += 1
        time.sleep(5.0 * self._restarts)  # бэкофф как у supervised()
        if self._starting.acquire(blocking=False):
            try:
                self._spawn_and_wait()
            finally:
                self._starting.release()

    def _kill_proc(self) -> None:
        proc, self._proc = self._proc, None
        if proc is None or proc.poll() is not None:
            return
        try:
            proc.stdin.close()  # EOF: worker выходит сам
            proc.terminate()
            proc.wait(timeout=5)
        except (OSError, subprocess.TimeoutExpired):
            proc.kill()

    def stop(self) -> None:
        self._ready = False
        self._kill_proc()
        self._http.close()
```

- [x] **Step 2: обновить `__init__.py`** — добавить импорт/экспорт `S1MiniTTS` (см. Interfaces Task 2 Step 5).

- [x] **Step 3: верификация**

```
python -m py_compile src/stream_director/tts/client.py
python -c "import sys; sys.path.insert(0,'src'); from stream_director.tts import S1MiniTTS; t = S1MiniTTS(on_status=print); print('available:', t.available); t.stop()"
```

Expected: печать статуса `{'state': 'checking', ...}`, `available: False`, без исключений.

---

### Task 5: worker.py — подпроцесс с моделью

**Files:**
- Create: `src/stream_director/tts/worker.py`

**Interfaces:**
- Consumes: только stdlib + пакеты из `--runtime`; фактические импорты fish_speech — из `docs/superpowers/plans/spike-results.md` (Task 1); аргументы CLI из Task 4: `--runtime --model-dir --voices-dir --port`.
- Produces: HTTP на `127.0.0.1:<port>`:
  - `GET /health` → `{"phase": "loading"|"ready", "vram_mb": int}`
  - `POST /synth` `{"text": str, "voice": str|null}` → `200 audio/wav` | `503` (не готов) | `500 {"error": str}`

- [x] **Step 1: написать модуль** (если импорты в `spike-results.md` отличаются — использовать их)

```python
"""Голосовой worker: отдельный процесс, держит S1-mini в VRAM.

Запускается client.py. Ничего не импортирует из stream_director: только
stdlib и gpu-runtime (путь приходит аргументом). Умирает по EOF stdin —
так осиротевший процесс не удержит видеопамять, даже если родителя убили.
"""

from __future__ import annotations

import argparse
import json
import os
import sys
import threading
import wave
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer
from io import BytesIO
from pathlib import Path

PHASE = {"value": "loading"}
ENGINE = None
LOCK = threading.Lock()  # один синтез за раз — как у Silero раньше
ARGS = None


def watch_parent_stdin() -> None:
    sys.stdin.buffer.read()  # блокируется до смерти родителя
    os._exit(0)


def load_engine():
    import torch
    from fish_speech.inference_engine import TTSInferenceEngine
    from fish_speech.models.dac.inference import load_model as load_decoder_model
    from fish_speech.models.text2semantic.inference import launch_thread_safe_queue

    precision = torch.bfloat16
    llama_queue = launch_thread_safe_queue(
        checkpoint_path=str(ARGS.model_dir), device="cuda",
        precision=precision, compile=False,
    )
    decoder = load_decoder_model(
        config_name="modded_dac_vq",
        checkpoint_path=str(Path(ARGS.model_dir) / "codec.pth"), device="cuda",
    )
    return TTSInferenceEngine(
        llama_queue=llama_queue, decoder_model=decoder,
        precision=precision, compile=False,
    )


def synth_wav(text: str, voice: str | None) -> bytes:
    import numpy as np
    from fish_speech.utils.schema import ServeReferenceAudio, ServeTTSRequest

    references = []
    if voice and voice != "default":
        wav_p = Path(ARGS.voices_dir) / f"{voice}.wav"
        txt_p = Path(ARGS.voices_dir) / f"{voice}.txt"
        if wav_p.is_file() and txt_p.is_file():
            references = [ServeReferenceAudio(
                audio=wav_p.read_bytes(),
                text=txt_p.read_text(encoding="utf-8"),
            )]
    request = ServeTTSRequest(
        text=text, references=references, format="wav", streaming=False,
        max_new_tokens=1024, chunk_length=300,
        top_p=0.8, repetition_penalty=1.1, temperature=0.8,
    )
    sample_rate, parts = 44100, []
    with LOCK:
        for result in ENGINE.inference(request):
            if result.code == "error":
                raise RuntimeError(str(result.error))
            if result.audio is not None:
                sample_rate, data = result.audio
                parts.append(data)
    if not parts:
        raise RuntimeError("модель не вернула аудио")
    audio = np.concatenate(parts)
    pcm = (np.clip(audio, -1.0, 1.0) * 32767).astype("<i2").tobytes()
    buf = BytesIO()
    with wave.open(buf, "wb") as w:
        w.setnchannels(1)
        w.setsampwidth(2)
        w.setframerate(int(sample_rate))
        w.writeframes(pcm)
    return buf.getvalue()


class Handler(BaseHTTPRequestHandler):
    def log_message(self, *_):  # не засорять stderr родителя
        pass

    def _json(self, code: int, payload: dict) -> None:
        body = json.dumps(payload).encode()
        self.send_response(code)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def do_GET(self):
        if self.path != "/health":
            return self._json(404, {"error": "not found"})
        vram = 0
        if PHASE["value"] == "ready":
            import torch
            vram = torch.cuda.memory_allocated() // 2**20
        self._json(200, {"phase": PHASE["value"], "vram_mb": vram})

    def do_POST(self):
        if self.path != "/synth":
            return self._json(404, {"error": "not found"})
        if PHASE["value"] != "ready":
            return self._json(503, {"error": "модель ещё загружается"})
        try:
            length = int(self.headers.get("Content-Length", "0"))
            req = json.loads(self.rfile.read(length))
            data = synth_wav(req["text"], req.get("voice"))
        except Exception as e:  # любой сбой синтеза — это 500, а не смерть worker-а
            return self._json(500, {"error": str(e)})
        self.send_response(200)
        self.send_header("Content-Type", "audio/wav")
        self.send_header("Content-Length", str(len(data)))
        self.end_headers()
        self.wfile.write(data)


def main() -> None:
    global ARGS, ENGINE
    parser = argparse.ArgumentParser()
    parser.add_argument("--runtime", required=True)
    parser.add_argument("--model-dir", required=True)
    parser.add_argument("--voices-dir", required=True)
    parser.add_argument("--port", type=int, required=True)
    ARGS = parser.parse_args()

    # gpu-runtime первым в sys.path: его пины важнее site-packages приложения.
    sys.path.insert(0, str(Path(ARGS.runtime).resolve()))
    threading.Thread(target=watch_parent_stdin, daemon=True).start()

    # health отвечает «loading» уже во время разворота модели в VRAM.
    server = ThreadingHTTPServer(("127.0.0.1", ARGS.port), Handler)
    threading.Thread(target=server.serve_forever, daemon=True).start()

    ENGINE = load_engine()
    PHASE["value"] = "ready"
    threading.Event().wait()  # живём, пока не убьют / EOF stdin


if __name__ == "__main__":
    main()
```

- [x] **Step 2: верификация синтаксиса** (полный прогон — Task 12, нужны веса)

```
python -m py_compile src/stream_director/tts/worker.py
```

Expected: без вывода.

---

### Task 6: интеграция в main.py и статусы

**Files:**
- Modify: `src/stream_director/main.py:29` (импорт), `main.py:137-144` (load_tts), `main.py:194-204` (finally)

**Interfaces:**
- Consumes: `S1MiniTTS` (Task 4).
- Produces: `ctx.statuses["tts_state"]: dict` (формат из Global Constraints), `ctx.statuses["tts_status"]: str` (= state, читает smoke_test и бейдж панели).

- [x] **Step 1: заменить импорт** `from .tts import SileroTTS` → `from .tts import S1MiniTTS`.

- [x] **Step 2: заменить блок загрузки TTS** (строки 137–144) на:

```python
    # Голос: bootstrap (GPU-проверка, докачки) и запуск worker-а — в фоне,
    # сервер и текстовые реплики доступны сразу.
    def set_tts_status(st: dict) -> None:
        ctx.statuses["tts_state"] = st
        ctx.statuses["tts_status"] = st["state"]

    tts = S1MiniTTS(on_status=set_tts_status)
    broadcaster.tts = tts
    asyncio.get_running_loop().run_in_executor(None, tts.start)
```

- [x] **Step 3: выгрузка при закрытии** — в `finally` блока `run()` первой строкой добавить `tts.stop()` (до `director.stop()`: голос не нужен раньше всех, а VRAM освобождается мгновенно).

- [x] **Step 4: верификация**

```
python -m py_compile src/stream_director/main.py
python -c "import sys; sys.path.insert(0,'src'); import stream_director.main; print('ok')"
```

Expected: `ok`. (Импорт `stream_director.main` подтянет `tts.client`/`broadcast` — цепочка импортов целиком.)

---

### Task 7: broadcast.py — маркеры и новый клиент; удаление старого tts.py

**Files:**
- Modify: `src/stream_director/broadcast.py`
- Delete: `src/stream_director/tts.py`

**Interfaces:**
- Consumes: `AudioStore`, `S1MiniTTS`, `pick_voice`, `strip_markers` из пакета `stream_director.tts` (Tasks 2, 4).

- [x] **Step 1: импорты** — заменить `from .tts import AudioStore, SileroTTS, pick_voice` на `from .tts import AudioStore, S1MiniTTS, pick_voice, strip_markers`; тип поля `tts: SileroTTS | None = None` → `tts: S1MiniTTS | None = None`.

- [x] **Step 2: очистка текста для оверлея** — в `publish()` строку

```python
            "text": text if self.settings.text_enabled else "",
```

заменить на:

```python
            # Зрителю — текст без эмо-маркеров; в синтез уходит оригинал.
            "text": strip_markers(text) if self.settings.text_enabled else "",
```

`_send_audio` не меняется — synth получает `text` с маркерами.

- [x] **Step 3: удалить `src/stream_director/tts.py`** (его заменил пакет `tts/`).

- [x] **Step 4: верификация**

```
python -m py_compile src/stream_director/broadcast.py
python -c "import sys; sys.path.insert(0,'src'); from stream_director.broadcast import OverlayBroadcaster; print('ok')"
```

Expected: `ok`. ✓ пройдено

---

### Task 8: server.py — API голосов, retry, статус

**Files:**
- Modify: `src/stream_director/server.py:28` (импорт), `:274-311` (status/voices/preview), добавить эндпоинты
- Modify: `pyproject.toml` (dependency `python-multipart` — нужен FastAPI для Form/File)

**Interfaces:**
- Consumes: `DEFAULT_VOICE`, `list_voices`, `save_voice`, `delete_voice` (Task 2); `ctx.broadcaster.tts: S1MiniTTS` (Tasks 4, 6).
- Produces (для панели, Task 10):
  - `GET /api/voices` → `{"voices": ["default", ...]}` (как раньше, но динамический список)
  - `POST /api/voices` multipart: `name: str, transcript: str, file: UploadFile` → 201 | 400
  - `DELETE /api/voices/{name}` → 200 | 404 | 400 (default)
  - `POST /api/tts/retry` → 202 (повторный `start()` в executor)
  - `/api/status`: поле `tts: bool` остаётся; `tts_state`/`tts_status` приходят из `ctx.statuses`

- [x] **Step 1: импорт** — заменить `from .tts import VOICES` на `from .tts import DEFAULT_VOICE, delete_voice, list_voices, save_voice`; в строке импортов FastAPI добавить `File, Form, UploadFile`.

- [x] **Step 2: `/api/voices` и новые эндпоинты** — заменить существующий `list_voices` эндпоинт и добавить:

```python
    @app.get("/api/voices")
    async def get_voices():
        return {"voices": list_voices()}

    @app.post("/api/voices", status_code=201)
    async def add_voice(
        name: str = Form(...), transcript: str = Form(...), file: UploadFile = File(...)
    ):
        data = await file.read()
        if len(data) > 15 * 2**20:
            raise HTTPException(400, "референс больше 15 МБ")
        if not data.startswith(b"RIFF"):
            raise HTTPException(400, "нужен WAV-файл (5–12 секунд чистой речи)")
        try:
            save_voice(name, data, transcript)
        except ValueError as e:
            raise HTTPException(400, str(e))
        return {"ok": True}

    @app.delete("/api/voices/{name}")
    async def remove_voice(name: str):
        if name == DEFAULT_VOICE:
            raise HTTPException(400, "встроенный голос не удаляется")
        if not delete_voice(name):
            raise HTTPException(404, "голос не найден")
        return {"ok": True}

    @app.post("/api/tts/retry", status_code=202)
    async def tts_retry():
        tts = ctx.broadcaster.tts
        if tts is None:
            raise HTTPException(503, "голос не инициализирован")
        asyncio.get_running_loop().run_in_executor(None, tts.start)
        return {"ok": True}
```

- [x] **Step 3: `/api/tts/preview`** — без изменений логики (интерфейс `synth` совпадает). Проверить, что упоминаний `VOICES` в файле не осталось.

- [x] **Step 4: `pyproject.toml`** — в `dependencies` добавить строку `"python-multipart>=0.0.9",` после `"numpy>=1.26",`.

- [x] **Step 5: верификация**

```
python -m pip install python-multipart
python -m py_compile src/stream_director/server.py
python -c "import sys; sys.path.insert(0,'src'); from stream_director.server import create_app; print('ok')"
```

Expected: `ok`.

---

### Task 9: эмо-инструкция в response_format

**Files:**
- Modify: `src/stream_director/commentary/defaults.py:23-33`
- Modify: `src/stream_director/config.py:51` (`default_voice: str = "default"`)

**Interfaces:**
- Consumes: `EMOTION_MARKERS` (Task 2) — единый источник списка, промпт не разъедется с вырезанием.

- [x] **Step 1: `defaults.py`** — добавить импорт `from ..tts.markers import EMOTION_MARKERS` и к константе `RESPONSE_FORMAT` дописать хвост (конкатенация после существующего текста):

```python
RESPONSE_FORMAT = (
    "Правила ответа: одна реплика, ОДНО короткое предложение (не длиннее ~10 слов), "
    "по-русски, без кавычек и пояснений, без хэштегов и эмодзи. Только сама реплика. "
    "Коротко и хлёстко лучше, чем длинно и витиевато.\n"
    "Не повторяйся:\n"
    "- Обращение к стримеру КАЖДЫЙ РАЗ новое. Одно и то же прозвище во всех репликах "
    "(например, звать его «маэстро» снова и снова) — недопустимо. Чередуй: ироничный "
    "титул, роль в бою, обыгрывание техники или чемпиона, прямое «ты», иногда — вообще "
    "без обращения.\n"
    "- Избегай штампов: «ну что ж», «классика жанра», «как всегда».\n"
    "Эмоция голоса: можешь поставить В НАЧАЛЕ реплики один маркер из списка — "
    + " ".join(f"({m})" for m in EMOTION_MARKERS)
    + " — если он усиливает подачу. Зритель маркер не увидит, он меняет только "
    "интонацию озвучки. Не больше одного, других не выдумывай."
)
```

- [x] **Step 2: `config.py`** — `default_voice: str = "baya"` → `default_voice: str = "default"`; комментарий над блоком голосов поправить: имена — референсы из `data/voices/`, `"default"` — голос модели.

- [x] **Step 3: верификация**

```
python -c "import sys; sys.path.insert(0,'src'); from stream_director.commentary.defaults import RESPONSE_FORMAT; assert '(angry)' in RESPONSE_FORMAT and '(sighing)' in RESPONSE_FORMAT; from stream_director.config import Settings; assert Settings().default_voice == 'default'; print('ok')"
```

Expected: `ok`. Примечание: у пользователей, сохранивших кастомный response_format в БД, инструкция появится после «Сбросить формат к заводскому» — это штатное поведение хранилища промптов.

---

### Task 10: панель — лоадеры и управление голосами

**Files:**
- Modify: `web/src/shared/api.ts`
- Modify: `web/src/panel/Panel.tsx`
- Modify: `web/src/panel/panel.css`

**Interfaces:**
- Consumes: API из Task 8; `status.tts_state` из Task 6.

- [x] **Step 1: `api.ts`** — добавить тип и методы:

```ts
export interface TtsState {
  state:
    | "checking" | "no_gpu" | "downloading_runtime" | "downloading_model"
    | "starting" | "loading" | "ready" | "error";
  progress?: { done_mb?: number; total_mb?: number; step?: string } | null;
  error?: string | null;
}
```

В `StatusDto` добавить поле `tts_state?: TtsState;`. В объект `api` добавить:

```ts
  uploadVoice: (name: string, transcript: string, file: File) => {
    const form = new FormData();
    form.append("name", name);
    form.append("transcript", transcript);
    form.append("file", file);
    // Без Content-Type: браузер сам ставит boundary для multipart.
    return fetch("/api/voices", { method: "POST", body: form }).then(async (r) => {
      if (!r.ok) throw new Error(`${r.status}: ${await r.text()}`);
    });
  },
  deleteVoice: (name: string) =>
    req(`/api/voices/${encodeURIComponent(name)}`, { method: "DELETE" }),
  ttsRetry: () => req("/api/tts/retry", { method: "POST" }),
```

- [x] **Step 2: `Panel.tsx` — компонент лоадера** (рядом с `Badge`):

```tsx
const TTS_STATE_TEXT: Record<string, string> = {
  checking: "Проверяю видеокарту…",
  downloading_runtime: "Скачиваю голосовой движок",
  downloading_model: "Скачиваю голосовую модель",
  starting: "Запускаю голосовой движок…",
  loading: "Разворачиваю модель в память видеокарты…",
};

function TtsLoader({ st, onRetry }: { st: TtsState; onRetry: () => void }) {
  if (st.state === "ready") return null;
  const pct =
    st.progress?.total_mb && st.progress?.done_mb !== undefined
      ? Math.round((st.progress.done_mb / st.progress.total_mb) * 100)
      : null;
  return (
    <div className={`tts-loader ${st.state === "error" || st.state === "no_gpu" ? "tts-loader-bad" : ""}`}>
      {st.state === "error" || st.state === "no_gpu" ? (
        <>
          <span>{st.error ?? "голос недоступен"}</span>
          <button onClick={onRetry}>повторить</button>
        </>
      ) : (
        <>
          <span>
            {TTS_STATE_TEXT[st.state] ?? st.state}
            {st.progress?.step && `: ${st.progress.step}`}
            {pct !== null && ` — ${st.progress!.done_mb} / ${st.progress!.total_mb} МБ`}
          </span>
          {pct !== null && (
            <div className="tts-progress"><div style={{ width: `${pct}%` }} /></div>
          )}
        </>
      )}
    </div>
  );
}
```

- [x] **Step 3: `Panel.tsx` — подключить лоадер** после строки `{message && <div className="message">{message}</div>}`:

```tsx
      {status.tts_state && (
        <TtsLoader
          st={status.tts_state}
          onRetry={() =>
            api.ttsRetry().then(() => setMessage("Перезапускаю голос…")).catch((e) => setMessage(String(e)))
          }
        />
      )}
```

Импортировать `TtsState` из `../shared/api`.

- [x] **Step 4: `Panel.tsx` — загрузка голосов** в конец секции `<h2>Голос</h2>` (после блока «добавить оверрайд»):

```tsx
        <p className="hint">
          Свой голос: WAV 5–12 секунд чистой речи + её текст. «default» — голос модели.
        </p>
        <div className="row">
          <input placeholder="имя голоса" value={newVoiceName}
                 onChange={(e) => setNewVoiceName(e.target.value)} />
          <input type="file" accept=".wav" onChange={(e) => setNewVoiceFile(e.target.files?.[0] ?? null)} />
        </div>
        <textarea className="prompt" placeholder="текст, произнесённый в записи"
                  value={newVoiceText} onChange={(e) => setNewVoiceText(e.target.value)} />
        <div className="row">
          <button
            onClick={() => {
              if (!newVoiceName.trim() || !newVoiceFile || !newVoiceText.trim()) return;
              api.uploadVoice(newVoiceName.trim(), newVoiceText, newVoiceFile)
                .then(() => {
                  setNewVoiceName(""); setNewVoiceText(""); setNewVoiceFile(null);
                  setMessage("Голос сохранён");
                  api.getVoices().then((v) => setVoices(v.voices)).catch(() => {});
                })
                .catch((e) => setMessage(String(e)));
            }}
          >
            Добавить голос
          </button>
          {voices.filter((v) => v !== "default").map((v) => (
            <button key={v} className="danger"
              onClick={() =>
                api.deleteVoice(v).then(() => api.getVoices().then((r) => setVoices(r.voices))).catch((e) => setMessage(String(e)))
              }
            >
              удалить «{v}»
            </button>
          ))}
        </div>
```

И состояния рядом с остальными `useState`:

```tsx
  const [newVoiceName, setNewVoiceName] = useState("");
  const [newVoiceText, setNewVoiceText] = useState("");
  const [newVoiceFile, setNewVoiceFile] = useState<File | null>(null);
```

- [x] **Step 5: `panel.css`** — добавить в конец:

```css
.tts-loader {
  display: flex; flex-direction: column; gap: 6px;
  padding: 10px 14px; margin-bottom: 12px; border-radius: 6px;
  background: rgba(96, 165, 250, 0.12); font-size: 14px;
}
.tts-loader-bad { background: rgba(248, 113, 113, 0.12); }
.tts-loader-bad button { align-self: flex-start; }
.tts-progress { height: 6px; border-radius: 3px; background: rgba(255,255,255,0.12); overflow: hidden; }
.tts-progress > div { height: 100%; background: #60a5fa; transition: width 0.4s; }
```

- [x] **Step 6: верификация**

```
cd web && npm run build
```

Expected: сборка без ошибок TypeScript.

---

### Task 11: зеркало весов + финальные пины (требует действий пользователя)

HF-репозиторий `fishaudio/openaudio-s1-mini` гейтится — нужен логин пользователя и принятие лицензии на странице модели.

**Files:**
- Modify: `src/stream_director/tts/pins.py` (sha256, размеры, версии)

**Interfaces:**
- Consumes: аккаунт HuggingFace пользователя; `gh` CLI с правами на репозиторий.
- Produces: заполненный `WEIGHTS` в `pins.py`; GitHub-релиз `models-s1-mini-v1` с пятью ассетами.

- [x] **Step 1 (пользователь): принять лицензию** — НЕ ПОНАДОБИЛОСЬ: те же веса
  официально выложены fishaudio на ModelScope и отдаются без регистрации
  (проверено: HF даёт 401, ModelScope — 200; размеры совпадают с HF байт в байт).

- [x] **Step 2: скачать веса** — скачаны 2026-07-12 с
  `https://modelscope.cn/models/fishaudio/openaudio-s1-mini/resolve/master/<файл>`
  в `build/s1-mini/` (5 файлов, 3.6 ГБ).

- [x] **Step 3: создать релиз-зеркало** — опубликован 2026-07-12 по явному
  подтверждению пользователя; все 5 ассетов доступны анонимно, размеры точные,
  Range-докачка подтверждена (HTTP 206):

```
gh release create models-s1-mini-v1 --title "OpenAudio S1-mini weights (mirror)" --notes "Зеркало весов OpenAudio S1-mini © Fish Audio, лицензия CC-BY-NC-SA 4.0 (некоммерческое использование). Источник: https://huggingface.co/fishaudio/openaudio-s1-mini" build/s1-mini/model.pth build/s1-mini/codec.pth build/s1-mini/tokenizer.tiktoken build/s1-mini/special_tokens.json build/s1-mini/config.json
```

- [x] **Step 4: вписать sha256 и размеры в `pins.py`** — вписаны 2026-07-12
  (посчитаны по скачанным файлам); версии пакетов вписаны ранее из spike-results.md.

```
python -c "import hashlib, pathlib
for p in sorted(pathlib.Path('build/s1-mini').glob('*')):
    if p.suffix in ('.pth', '.tiktoken', '.json'):
        h = hashlib.sha256(p.read_bytes()).hexdigest()
        print(f'    \"{p.name}\": (\"{h}\", {p.stat().st_size}),')"
```

Вывод вставить в `WEIGHTS` в `pins.py`. Туда же — финальные версии пакетов из `spike-results.md`, если Task 2 оставил `<из spike>`.

- [x] **Step 5: верификация** — `ok`, 2026-07-12.

```
python -c "import sys; sys.path.insert(0,'src'); from stream_director.tts.pins import WEIGHTS, RUNTIME_PACKAGES; assert all(sha and size for sha, size in WEIGHTS.values()); assert not any('<' in p for p in RUNTIME_PACKAGES); print('ok')"
```

Expected: `ok`.

---

### Task 12: сборка, README, smoke-test

**Files:**
- Modify: `scripts/build_portable.py`
- Modify: `scripts/smoke_test.py:59-67`
- Modify: `README.md`
- Modify: `pyproject.toml` (удалить extra `ml`)

**Interfaces:**
- Consumes: состояния `tts_status` из Task 6.

- [x] **Step 1: `build_portable.py`** — удалить константы `SILERO_MODEL_URL`, `SILERO_MODEL_SHA256`, функцию `fetch_model()` и её вызов в `main()`; в `build_python()` заменить `".[ml]"` на `"."`; после установки зависимостей добавить установку pip (нужен бутстраперу на машине пользователя):

```python
    # pip нужен bootstrap-у для установки gpu-runtime на машине пользователя.
    subprocess.run(
        [sys.executable, "-m", "pip", "install", "--target", str(site), "pip"],
        check=True,
    )
```

Обновить докстринг модуля: модель Silero больше не кладётся, голосовой стек докачивается приложением при первом старте.

- [x] **Step 2: `pyproject.toml`** — удалить секцию `[project.optional-dependencies]` целиком (`ml` был единственным extra; torch больше не зависимость приложения).

- [x] **Step 3: `smoke_test.py`** — CI без GPU: `no_gpu` — успех сборки, `error`/`unavailable` — провал. Заменить строки 59–65 на:

```python
                tts = status.get("tts_status")
                if tts in ("ready", "no_gpu"):
                    # no_gpu — норма для CI-раннера: сборка живая, голос требует NVIDIA.
                    print(f"OK: сервер отвечает, версия верна, tts={tts}")
                    return 0
                if tts in ("error", "unavailable"):
                    print(f"FAIL: tts_status={tts} — движок не работает в сборке")
                    return 1
```

- [x] **Step 4: `README.md`** — раздел «Голос» переписать: требования (видеокарта NVIDIA, 6+ ГБ VRAM; при первом старте докачивается ~6.5 ГБ, на диске ~12 ГБ), свои голоса через панель (WAV 5–12 с + транскрипт), эмо-маркеры. Блок лицензии: «Озвучка — модель OpenAudio S1-mini © Fish Audio, лицензия CC-BY-NC-SA 4.0; проект некоммерческий, веса распространяются зеркалом в Releases с сохранением лицензии».

- [x] **Step 5: верификация**

```
python -m py_compile scripts/build_portable.py scripts/smoke_test.py
```

Expected: без вывода. Полная сборка — на этапе релиза (не в этом плане).

---

### Task 13: E2E-прогон

- [x] **Step 1:** skill `verify` — приложение с фейковыми источниками: реплика доходит до overlay-WS текстом (без GPU-зависимости), `/api/status` содержит `tts_state` с валидным `state`. Прогон 2026-07-12: 12/12 проверок OK (бриф, склейка, !dir, tts_state=error «зеркало весов не опубликовано», эмо-маркер вырезан из оверлея, /api/voices=["default"]).
- [ ] **Step 2 (машина с GPU, вручную):** удалить `gpu-runtime/` и `models/` → холодный старт → в панели видны оба лоадера (пакеты → проценты весов → «разворачиваю в память видеокарты») → бейдж «голос» зелёный → превью голоса `default` играет → реплика с маркером `(angry)` озвучена со злой интонацией, а в оверлее текст без маркера → загрузка своего голоса работает → закрыть приложение → `nvidia-smi`: процессов python с занятой VRAM нет.
- [ ] **Step 3:** отчёт по чеклисту — что прошло, что нет.

---

## Self-Review (выполнен)

- **Spec coverage:** архитектура/раскладка (T2-T5), bootstrap-этапы и лоадеры (T3, T6, T10), worker и выгрузка VRAM (T4, T5, T6), голоса (T2, T8, T10), эмоции (T2, T9), ошибки (T3, T4, T8, T10), сборка/лицензия (T11, T12), верификация (T13), spike (T1). Пробелов не найдено.
- **Placeholder scan:** `<из spike>` в pins.py — осознанный sentinel с проверкой в Task 11 Step 5 и защитой в bootstrap (BootstrapError при пустом sha); остальное — конкретный код.
- **Type consistency:** статус-словарь `{state, progress, error}` един (bootstrap._st, client._set, api.ts TtsState); `synth(text, voice) -> bytes|None` совпадает у client и вызова в broadcast; CLI-аргументы worker совпадают со спавном в client; `list_voices()` включает `default` и в сервере, и в pick_voice.
